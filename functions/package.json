{
  "name": "functions",
  "scripts": {
    "build": "echo 'no build'",
    "deploy": "firebase deploy --only functions,firestore,storage,database"
  },
  "engines": { "node": "18" },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  }
}
